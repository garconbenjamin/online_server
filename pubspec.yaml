name: online_server
description: An new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.0.0
  postgres: ^2.6.3
  shelf_cors_headers: ^0.1.5
  stormberry: ^0.13.1

dev_dependencies:
  build_runner: ^2.4.7
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
